
---

## 20:02 修正：撤回 4 批提交，只提交源码路径同步（`64aebf2`）

用户指示「只提交源码，先不要提交文件，文件还要改的」——上一轮的 4 批提交（`b39a801` Prettier 配置 / `9f326ef` 路径同步 / `21b8e8c` Docker / `327f8ce` docs 目录）已 `git reset HEAD~4` 整体撤回，改动全部回到工作区。

随后只把 **7 个 modified 源码文件**（路径引用同步 `docs/architecture.md` → `docs/02-architecture/architecture.md`：`app/(dashboard)/layout.tsx`、`eslint.config.mjs`、`lib/{env,errors,issues,permissions,validation}.ts`）重新提交为 **`64aebf2`**。

**当前工作区（等待用户改完再提交）**：
- untracked：`docs/`（30 文件）、`Dockerfile`、`docker-compose.yml`、`docker-compose.prod.yml`、`docker/entrypoint.sh`、`.dockerignore`、`.prettierrc.json`、`.prettierignore`
- 这些文件用户还要继续改，**不要主动提交**

**HEAD = `64aebf2`**，验证结论不变（上一轮已验证）：Jest 20/20、ESLint 0 错、mock 配置冒烟 37/37、生产配置 32–33/37。
